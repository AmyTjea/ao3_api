version: '3.8'

services:
  fastapi-app:
    build:
      context: .
      dockerfile: Dockerfile.prod  
    ports:
      - "8000:80"
    environment:
      - ENVIRONMENT=production
    restart: always
